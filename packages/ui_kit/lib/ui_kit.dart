/// Assen Platform design system: ThemeData and shared widgets.
///
/// P0 placeholder. ASS-88 replaces this with the full light-mode ThemeData and
/// the core widget catalogue derived from `docs/design/` (tokens, components).
/// Apps must theme through this package rather than hard-coding colours.
library;

export 'src/theme.dart';
